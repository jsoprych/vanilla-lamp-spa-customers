{
  "name": "customer-spa",
  "version": "1.0.0",
  "scripts": {
    "clean": "rm -rf dist/ || true",
    "prebuild": "npm run clean && mkdir -p dist/assets/css dist/assets/js dist/api",
    "build": "tsc && ls -l dist/assets/js/ && cp src/css/styles.css dist/assets/css/ && cp src/index.html dist/ && cp -r server/api/. dist/api/",
    "start": "php -S localhost:8000 -t dist/"
  },
  "devDependencies": {
    "typescript": "5.8.3"
  }
}
